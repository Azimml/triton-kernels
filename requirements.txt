# Core dependencies
# The kernels use Triton 3.x idioms, so 3.0 is the real minimum (not 2.1).
torch>=2.1.0
triton>=3.7.1

# Testing
pytest>=7.0.0
pytest-benchmark>=4.0.0

# Benchmarking and visualization
matplotlib>=3.5.0
pandas>=1.4.0

# Linting and type checking (CPU-side CI)
ruff>=0.6
mypy>=1.10
